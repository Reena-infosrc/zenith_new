# Document renamed

The DynamoDB field encryption design has been renamed for clarity:

**→ [dynamodb-field-encryption-design.md](./dynamodb-field-encryption-design.md)**

That file includes the inventory aligned with `backend/serverless.yml` and production environment variable names (e.g. `DYNAMODB_TABLE_*`, `SERVICE_NAME`).
